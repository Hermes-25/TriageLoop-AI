# Infrastructure Boundary

Planned local workflow:

- one command starts the web application, API and prototype database;
- clean seeded reset for demonstrations;
- health checks for web/API;
- Docker-compatible packaging;
- no cloud dependency for the authoritative demo path.

Public hosting is useful but optional. A reproducible local prototype and recorded demonstration remain the fallback.

