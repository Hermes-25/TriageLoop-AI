# TL-07.5 technical paper and deployment record

Evidence lock: 22 August 2026, Asia/Calcutta.

## Technical working paper

- Source: `paper/main.tex`
- Final PDF: `submission/TriageLoop_Technical_Working_Paper.pdf`
- Scope: problem-statement traceability, research lineage, system and ML architecture, mathematical formulation, data generation, model selection, conformal/OOD safety, Action Windows, NBO, Queue Twin and Clinical Slack, product/API/audit design, validation, independent-review dispositions, governance, deployment, limitations and appendices.
- Figures: eight generated vector figures plus six product-state screenshots. The system architecture, online clinical-decision pipeline and offline ML development/validation pipeline each receive a dedicated full page; editable Mermaid sources are retained under `paper/diagrams/`.

## Verification lock

- Python: 73/73 tests passed on 22 August 2026.
- Web: TypeScript and optimized Next.js production build passed in Vercel's build environment.
- Public browser journey: passed with zero console and page errors.
- Captures: `artifacts/release/tl07-live/01-baseline.png` through `08-capacity.png`.
- Machine-readable result: `artifacts/release/tl07-live/verification.json`.
- Docker machine-readable result: `artifacts/release/tl07-docker-verification.json`.

## Vercel production deployment

- Public URL: <https://triageloop-ai.vercel.app/board>
- Deployment ID: `dpl_Engkgw53P3J8XbonHm2bWbFb1vY1`
- Deployment state: `READY`, production.
- Health response: `{"status":"ok","mode":"synthetic-presentation-adapter","version":"0.4.0-vercel"}`.

The hosted preview uses canonical precomputed snapshots exported from the FastAPI prototype. It keeps only small synthetic scenario/decision flags in an HTTP-only, same-site cookie and exposes the complete jury interaction path. It is not represented as the live Python scoring service. Local/Docker execution remains authoritative for live scoring, Queue Twin recomputation, persistent audit state and manual synthetic intake.

## Docker installation and passed gate

- Docker Desktop 4.87.0.236836 installed successfully.
- Docker Engine CLI 29.7.2 is present.
- Docker Compose 5.4.0 is present.
- WSL 2.7.12 is installed with WSL 2 as the default.
- `docker compose up --build -d` built the Python API and Next.js web images and started the health-gated stack.
- `/v1/health` returned `status=ok`, `mode=synthetic-prototype`, `version=0.4.0`; `/board` returned HTTP 200.
- The full browser journey passed locally with eight screenshots and zero console/page errors.
- A Compose restart preserved five audit events and the newest SHA-256 event hash exactly; integrity remained intact.
- `POST /v1/demo/reset` restored `baseline`, reduced the stream to one reset event and retained an intact chain.

The gate exposed two reproducibility defects that were corrected before acceptance: unreliable dependency downloads now use a persistent BuildKit pnpm cache with bounded retries, and the runner image now includes the resolved pnpm dependency tree required by Next.js standalone output. Docker Compose is the authoritative local reference path and remains running at `http://localhost:3000/board` unless stopped by the user.
